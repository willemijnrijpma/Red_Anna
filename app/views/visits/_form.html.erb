<%= form_for @visit do |f| %>

    <%= f.label :threat %>
    <%= f.number_field :threat %>

    <%= f.label :advice %>
    <%= f.number_field :advice %>

    <%= f.submit (@visit.new_record? ? "Create" : "Update") %>

<% end %>
