<%= form_for @visit do |f| %>
    <pre><%= @visit.errors.to_json %></pre>
    <%= f.label :threat %>
    <%= f.select :threat, [['Yes', true], ['No', false]] %>

    <%= f.label :advice %>
    <%= f.select :advice, [['Yes', true], ['No', false]] %>

    <%= f.label :hr %>
    <%= f.select :hr_id, Hr.select_options %>

    <%= f.submit (@visit.new_record? ? "Create" : "Update") %>

<% end %>
